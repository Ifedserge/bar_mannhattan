
export const dropMenu = (selector) => {

        const menuContainer = document.querySelectorAll(selector);

        if (menuContainer.length == 0) console.log('-');
        console.log( menuContainer);
    
}

